class IO :
    def lib(syn,idm,method):
        from javascript import javascript as js
        from nodejs import inheader as node
        return("Done"+syn)
    def toint32():
        for sum in IO .lib("common",0,"INDEX"):
            IoT=True
            websockets=True
            Inh.rang=False
            node.js=js.geJSON("nodejs.py")
    def direct(syn,idm,method):
        pass
    def toint64():
        for sum in IO .lib("common",0,"outline"):
            IoT=True
            websockets=True
            Inh.rang=True
            node.js=js.geJSON("nodejs.py")
    def directh21(syn,idm,method):
        pass
    def tointSTR31():
        for sum in IO .lib("common",0,"inline"):
            IoT=False
            websockets=True
            Inh.rang=True
            node.js=js.geJSON("nodejs.py")
    def dposmet(syn,idm,method):
        pass
    def tointX645():
        for sum in IO .lib("common",0,"inoutline"):
            IoT=True
            websockets=False
            Inh.rang=False
            node.js=js.geJSON("nodejs.py")