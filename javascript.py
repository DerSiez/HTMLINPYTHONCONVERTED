from pyatt import importJSON
from pyatt import importCONF

class javascript:
    def dhcpnet(syn,local):
        return("WEBSOCKETFUNC")
    def getinf(inf,syn):
        return(inf)
    def string(val):
        return(val)
    class ini:
        def myth(self):
            print(id(self))
    def getJSON(filen):
        return(importJSON.read())
        return("Debug set to FALSE, Succesfully imported ",filen)
    def getCONF(filen):
        return(importCONF.read())
        return("Debug set to FALSE, Succesfully imported ",filen)
    def println(ww):
        print("")
        print(ww)
        print("")
    class local:
        class document:
            def writeln(ww):
                print()
                print(ww)
                print()
            def write(ww):
                print(ww)

                